<button class="navbar-toggler" type="button" data-bs-toggle="{{ $type }}" data-bs-target="#{{ $target }}" aria-controls="{{ $target }}" aria-label="{{ $label ?? '' }}">
    <span class="navbar-toggler-icon"></span>
</button>
